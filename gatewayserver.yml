server:
  port: 8073

eureka:
  instance:
    preferredIpAddress: true
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: "http://localhost:8072/eureka/"